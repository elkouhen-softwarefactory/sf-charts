apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jenkins-ext.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "jenkins-ext.name" . }}
    helm.sh/chart: {{ include "jenkins-ext.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "jenkins-ext.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
