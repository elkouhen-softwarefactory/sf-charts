apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jenkinsext.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "jenkinsext.name" . }}
    helm.sh/chart: {{ include "jenkinsext.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "jenkinsext.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
